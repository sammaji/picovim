local settings = {
    hard_mode = false,
    auto_pairs = false,
    theme = {
        name = "kanagawa-wave",
        transparency = false,
        dimInactive = false, -- dims inactive window (if available)
    },
    workspace = {
        disable_neovim_syntax_features = false, -- disable neovim syntax feature (if you are working with large files.)
    },
}

return settings
